# Node
node_modules
.svelte-kit
build
dist
.vite
.env
.env.*
!.env.example

# Tauri
src-tauri/target
src-tauri/gen
src-tauri/WixTools

# Local-only cargo config (machine-specific target-dir; see file header).
# Used on this Linux box because the source lives on an NTFS/FUSE drive that
# can't exec build-scripts — would break the Windows build if committed.
src-tauri/.cargo/

# IDE
.vscode/*
!.vscode/extensions.json
.idea
*.swp

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Claude Code (local assistant docs/state — not part of the project)
CLAUDE.md
.claude/
.claude/**

# Scan exports (runtime output, not source)
scan-*.json
scan-*.csv

# Build artifacts / installers
*.rpm
*.deb
*.msi
*.AppImage
*.exe
